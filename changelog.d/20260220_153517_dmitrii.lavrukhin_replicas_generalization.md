### Changed

- \[Server API\] `POST api/tasks` request:
  field `consensus_replicas` removed in favor of the new `initial_replicas` field,
- \[Server API\] `GET api/jobs` and `GET api/jobs/{id}/` responses:
  `type` field value `CONSENSUS_REPLICA` renamed to `REPLICA`

  (<https://github.com/cvat-ai/cvat/pull/10300>)
