### Added

- \[CLI\] New `--file PATH` option on `cvat-cli profile create` that reads the
  Personal Access Token from a plain-text file or from the JSON envelope
  emitted by the web UI's "Download token" button
  (<https://github.com/cvat-ai/cvat/pull/XXXX>)
