### Changed

- Python tests on Kubernetes now use a pytest-managed Minikube and Helm runtime with explicit `simple`, `standard`, and `full` profiles, isolated kube-only test dependencies, and updated CI/docs to match the new kube flow.
