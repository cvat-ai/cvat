### Fixed

- PDF tasks created with an explicit `stop_frame` no longer contain one frame
  fewer than requested, and `stop_frame=0` no longer fails task creation with
  "No image found"
  (<https://github.com/cvat-ai/cvat/issues/11175>)
