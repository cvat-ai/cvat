tls:
  certificates:
    - certFile: /certs/your-crt.pem
      keyFile: /certs/your-key.pem
  stores:
    default:
      defaultCertificate:
        certFile: /certs/your-crt.pem
        keyFile: /certs/your-key.pem
