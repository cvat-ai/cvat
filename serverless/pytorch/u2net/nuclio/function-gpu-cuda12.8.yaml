metadata:
  name: u2net
  namespace: cvat
  annotations:
    name: U2Net
    type: detector
    spec: |
      [
        { "id": 0, "name": "anomaly", "type": "mask" }
      ]

spec:
  description: U2Net (GPU-enabled with CUDA 12.8)
  runtime: 'python:3.11'
  handler: main:handler
  eventTimeout: 3000s

  build:
    image: cvat.pth.u2net:cuda12.8
    baseImage: pytorch-unified:cuda12.8

    directives:
      preCopy:
        - kind: ENV
          value: DEBIAN_FRONTEND=noninteractive

        - kind: WORKDIR
          value: /opt/nuclio

        # U2Net code already in base image
        # Handler files at /opt/nuclio/u2net_main.py and u2net_model_handler.py
        - kind: RUN
          value: ln -sf /opt/nuclio/u2net_main.py /opt/nuclio/main.py && ln -sf /opt/nuclio/u2net_model_handler.py /opt/nuclio/model_handler.py

  resources:
    limits:
      nvidia.com/gpu: 1
    requests:
      cpu: "1"
      memory: "2Gi"

  triggers:
    myHttpTrigger:
      numWorkers: 2
      kind: 'http'
      workerAvailabilityTimeoutMilliseconds: 300000
      attributes:
        maxRequestBodySize: 33554432 # 32MB
        port: 32764

  platform:
    attributes:
      restartPolicy:
        name: always
        maximumRetryCount: 3
      mountMode: volume