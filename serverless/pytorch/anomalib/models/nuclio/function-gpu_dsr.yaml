metadata:
  name: dsr
  namespace: cvat
  annotations:
    name: dsr
    type: detector
    spec: |
      [
        { "id": 0, "name": "anomaly", "type": "mask" }
      ]

spec:
  description: dsr from Anomalib (GPU-enabled)
  runtime: 'python:3.11'
  handler: dsr.main:handler
  eventTimeout: 3000s

  build:
    image: cvat.pth.anomalib_aux.models:gpu
    baseImage: anomalib-base:ubuntu22.04  # Base with proper GL libraries and CUDA

    directives:
      preCopy:
        - kind: ENV
          value: DEBIAN_FRONTEND=noninteractive

        # 3. Install Python packages
        - kind: WORKDIR
          value: /opt/nuclio

  resources:
    limits:
      nvidia.com/gpu: 1
    requests:
      cpu: "1"
      memory: "2Gi"

  triggers:
    myHttpTrigger:
      numWorkers: 2
      kind: 'http'
      workerAvailabilityTimeoutMilliseconds: 300000
      attributes:
        maxRequestBodySize: 33554432 # 32MB
        port: 32752

  platform:
    attributes:
      restartPolicy:
        name: always
        maximumRetryCount: 3
      mountMode: volume