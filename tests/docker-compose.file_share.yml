version: '3.3'

services:
  cvat:
    environment:
      CVAT_SHARE_URL: 'Mounted from asset for case 106 host directory'
    volumes:
      - cvat_share:/home/django/share:rw

volumes:
  cvat_share:
    driver_opts:
      type: none
      device: tests/cypress/integration/actions_tasks3/assets/case_106
      o: bind
