version: '3.3'

services:
  cvat_worker_default:
    volumes:
      - ./tests/cypress/integration/share:/home/django/share:rw
  cvat_server:
    volumes:
      - ./tests/cypress/integration/share:/home/django/share:rw
