version: '3.3'

services:
  cvat_server:
    volumes:
      - cvat_share:/home/django/share:rw
  cvat_worker_default:
    volumes:
      - cvat_share:/home/django/share:rw

volumes:
  cvat_share:
    driver_opts:
      type: none
      device: tests/cypress/integration/actions_tasks3/assets/case_107
      o: bind
