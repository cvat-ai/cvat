version: '3.3'

services:
  cvat_worker_default:
    volumes:
      - ./tests/mounted_file_share:/home/django/share:rw
  cvat_server:
    volumes:
      - ./tests/mounted_file_share:/home/django/share:rw
