#!/bin/bash

set -e

# https://clickhouse.com/docs/sql-reference/statements/alter/column#add-column
# Adding a column is expected to be a lightweight call

echo "
ALTER TABLE ${CLICKHOUSE_DB}.events
ADD COLUMN IF NOT EXISTS
\`api_token_id\` Nullable(UInt64) DEFAULT NULL
;" | clickhouse-client
